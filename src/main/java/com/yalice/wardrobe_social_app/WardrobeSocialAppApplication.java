package com.yalice.wardrobe_social_app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for the Wardrobe Social App.
 * This Spring Boot application serves as the entry point for the application.
 */
@SpringBootApplication
public class WardrobeSocialAppApplication {

	/**
	 * Main method that starts the Spring Boot application.
	 *
	 * @param args Command line arguments passed to the application
	 */
	public static void main(String[] args) {
		SpringApplication.run(WardrobeSocialAppApplication.class, args);
	}

}
